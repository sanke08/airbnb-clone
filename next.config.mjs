/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "uploadthing.com",
          "utfs.io",
          "lh3.googleusercontent.com",
          "images.unsplash.com",
          "unsplash.com"
        ]
      }
};

export default nextConfig;
